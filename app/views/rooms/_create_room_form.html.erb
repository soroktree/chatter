<%= form_with model: @room do |f| %>
    <div class = "mb-3 ml-0 row">

        <%= f.submit "New room", class: 'btn btn-primary' %>

        <div class = "col-sm-10">
            <%= f.text_field :name, placeholder: "Name", class: 'form-control'%>
        </div>  
    </div>
    
<%end%>